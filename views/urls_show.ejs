<!DOCTYPE html>
<html lang='en'>
<head>
  <meta charset='utf-8'>
  <title>TinyApp</title>
  <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css'>
  <link rel='stylesheet' href='/stylesheets/my-css.css'>
</head>
<body>
  <div class='container'>
    <% include partials/_header %>
    <div class='panel panel-default'>
      <div class='panel-body'>
        <div class='well'>
          <h4>Short URL: <a href='/u/<%= url.shortURL %>'>http://localhost:3000/u/<%= url.shortURL %></a></h4>
          <span>Target URL: <%= url.longURL %></span>
          <span class='pull-right'>Visits: <%= url.visits %> Unique: <%= url.uniqueVisits %> | Created: <%= url.dateCreated %></span>
        </div>
        <table class='table'>
          <tbody>
            <tr>
              <td>
                <form method='POST' action='/urls/<%= url.shortURL %>?_method=PUT'>
                  <div class='input-group'>
                    <span class='input-group-addon'>Change Target URL:</span>
                      <input class='form-control' type='text' name='newLongURL' placeholder='<%= url.longURL %>'>
                    <span class='input-group-btn'>
                      <button class='btn btn-info' type='submit'>Update!</button>
                    </span>
                  </div>
                </form>
              </td>
              <td>
                <form method='POST' action='/urls/<%= url.shortURL %>/delete?_method=DELETE'>
                  <button class='btn btn-s btn-danger'>
                    <span class='glyphicon glyphicon-trash'></span>
                  </button>
                </form>
              </td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</body>
</html>