<!DOCTYPE html>
<html lang='en'>
<head>
  <title>TinyApp</title>
</head>
<body>
  <% include partials/_header %>
  <ul>
    <% for (let site in urls) { %>
        <li>
          <form method="POST" action="/urls/<%= site %>/delete">
            <%= urls[site] %> : <%= site %>   <input type="submit" value="DELETE">
          </form>
        </li>
    <% } %>
  </ul>
  <a href='#'>Shorten a new URL!</a>
</body>
</html>