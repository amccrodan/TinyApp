<!DOCTYPE html>
<html lang='en'>
<head>
  <title>TinyApp</title>
</head>
<body>
  <%- include('partials/_header') %>
  <ul>
    <% for (let site in urls) { %>
        <li><%= urls[site] %> : <%= site %></li>
    <% } %>
  </ul>
  <a href='#'>Shorten a new URL!</a>
</body>
</html>