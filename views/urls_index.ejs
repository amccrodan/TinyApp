<!DOCTYPE html>
<html lang='en'>
<head>
  <title>TinyApp</title>
</head>
<body>
  <% include partials/_header %>
  <ul>
    <% for (let shortURL in urls) { %>
        <li>
          <form method="POST" action="/urls/<%= shortURL %>/delete">
            <%= shortURL %> --> <%= urls[shortURL] %>    <input type="submit" value="DELETE">
          </form>
        </li>
    <% } %>
  </ul>
  <a href='/urls/new'>Shorten a new URL!</a>
</body>
</html>